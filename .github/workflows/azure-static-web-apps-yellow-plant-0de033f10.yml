name: Azure Static Web Apps CI/CD

on:
  push:
    branches:
      - main

jobs:
  build_and_deploy_job:
    runs-on: ubuntu-latest
    name: Build and Deploy Job
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: true

      # Ruby 3.1 のセットアップ（キャッシュは一旦OFF）
      - name: Set up Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.1'
          bundler-cache: false

      # Bundler バージョンを明示的に固定してインストール
      - name: Install specific Bundler
        run: |
          gem install bundler -v 2.6.9

      # バージョンを指定して Jekyll ビルドを実行
      - name: Build Jekyll
        run: |
          bundle _2.6.9_ install
          bundle _2.6.9_ exec jekyll build

      # Azure Static Web Apps へ成果物をアップロード
      - name: Deploy to Azure SWA
        id: builddeploy
        uses: Azure/static-web-apps-deploy@v1
        with:

        # Jekyll ビルドの実行（環境変数 LANG を追加）
      - name: Build Jekyll
        run: |
          bundle _2.6.9_ install
          bundle _2.6.9_ exec jekyll build
        env:
          LANG: ja_JP.UTF-8
          LANGUAGE: ja_JP:ja
          LC_ALL: ja_JP.UTF-8
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN_YELLOW_PLANT_0DE033F10 }}
          action: "upload"
          app_location: "/"
          api_location: ""
          output_location: "_site"
          skip_app_build: true
